## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## Verification

- [ ] `uvx ruff check .` is clean
- [ ] `uv lock --check` passes
- [ ] `uv run python scripts/check_dependencies.py` passes
- [ ] `uv run python scripts/check_imports.py` passes
- [ ] Ran `uv run pytest` on a Blackwell GPU, or: not applicable / no GPU available

## Notes

<!-- Benchmark numbers, hardware used, anything a reviewer should know. -->
